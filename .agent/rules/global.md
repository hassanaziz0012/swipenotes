---
trigger: always_on
---

- When designing new components, always try to reuse styles from the `constants/styles.ts` file. 
- When creating new components, try to reuse existing components from the `components` folder. If possible, also create new reusable components inside that folder. 